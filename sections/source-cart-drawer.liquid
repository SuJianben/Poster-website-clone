{% if section.settings.enabled %}
{% comment %}The storefront cart drawer is mounted once by source-global-overlays.{% endcomment %}
{% endif %}
{% schema %}
{
  "name": "Cart drawer",
  "settings": [
    { "type": "checkbox", "id": "enabled", "label": "Show section", "default": true }
  ],
  "presets": [{ "name": "Cart drawer" }]
}
{% endschema %}
