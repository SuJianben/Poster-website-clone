{% if section.settings.enabled %}
{% render 'source-header-promotion-1', section: section %}
{% endif %}
{% schema %}
{
  "name": "页头促销栏",
  "settings": [
    { "type": "checkbox", "id": "enabled", "label": "显示模块", "default": true }
  ],
  "blocks": [
    {
      "type": "text",
      "name": "促销文字",
      "settings": [
        { "type": "text", "id": "text", "label": "文字内容", "default": "✨ 5% - 25% Rabatt Sommer Sale" }
      ]
    }
  ],
  "presets": [{ "name": "页头促销栏" }]
}
{% endschema %}
