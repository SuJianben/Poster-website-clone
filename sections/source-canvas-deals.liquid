{% if section.settings.enabled %}
{% render 'source-canvas-deals-1' %}
{% endif %}
{% schema %}
{
  "name": "Canvas deals",
  "settings": [
    { "type": "checkbox", "id": "enabled", "label": "Show section", "default": true }
  ],
  "presets": [{ "name": "Canvas deals" }]
}
{% endschema %}