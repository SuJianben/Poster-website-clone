{% if section.settings.enabled %}
  {% render 'source-announcement-1' %}
  {% if section.settings.heading != blank or section.settings.review_count != blank or section.blocks.size > 0 %}
    <script type="application/json" data-source-announcement-overrides>
      {
        "heading": {{ section.settings.heading | json }},
        "reviewCount": {{ section.settings.review_count | json }},
        "reviews": [
          {% for block in section.blocks %}
            {% assign product = block.settings.product %}
            {
              "index": {{ forloop.index0 }},
              "blockId": {{ block.id | json }},
              "shopifyAttributes": {{ block.shopify_attributes | json }},
              "date": {{ block.settings.date | json }},
              "rating": {{ block.settings.rating | json }},
              "title": {{ block.settings.title | json }},
              "message": {{ block.settings.message | json }},
              "author": {{ block.settings.author | json }},
              "link": {{ block.settings.link | json }},
              "product": {% if product != blank %}{
                "url": {{ product.url | json }},
                "title": {{ product.title | json }},
                "image": {% if block.settings.product_image != blank %}{{ block.settings.product_image | image_url: width: 160 | json }}{% elsif product.featured_image != blank %}{{ product.featured_image | image_url: width: 160 | json }}{% else %}null{% endif %}
              }{% else %}null{% endif %}
            }{% unless forloop.last %},{% endunless %}
          {% endfor %}
        ]
      }
    </script>
  {% endif %}
{% endif %}
{% schema %}
{
  "name": "客户评价",
  "settings": [
    { "type": "checkbox", "id": "enabled", "label": "显示模块", "default": true },
    { "type": "text", "id": "heading", "label": "自定义标题" },
    { "type": "text", "id": "review_count", "label": "自定义评价数量" }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "评价内容",
      "settings": [
        { "type": "paragraph", "content": "区块按当前顺序覆盖原始评价卡片第 1 至 6 项；字段留空则保留原始内容。" },
        { "type": "text", "id": "date", "label": "评价日期" },
        { "type": "range", "id": "rating", "label": "星级", "min": 1, "max": 5, "step": 1, "default": 5 },
        { "type": "text", "id": "title", "label": "评价标题" },
        { "type": "textarea", "id": "message", "label": "评价内容" },
        { "type": "text", "id": "author", "label": "客户姓名" },
        { "type": "product", "id": "product", "label": "关联商品" },
        { "type": "image_picker", "id": "product_image", "label": "自定义商品图片" },
        { "type": "url", "id": "link", "label": "自定义评价链接" }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [{ "name": "客户评价" }]
}
{% endschema %}
