{% if section.settings.enabled %}
{% render 'source-promotion-1' %}
{% endif %}
{% schema %}
{
  "name": "Promotion",
  "settings": [
    { "type": "checkbox", "id": "enabled", "label": "Show section", "default": true }
  ],
  "presets": [{ "name": "Promotion" }]
}
{% endschema %}