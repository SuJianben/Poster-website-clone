{{ 'source-cart-drawer.css' | asset_url | stylesheet_tag }}

<cart-drawer
  id="CartDrawer"
  class="drawer cart-drawer drawer--right"
  data-section-id="source-cart-drawer"
  hidden
>
  <div class="fixed-overlay" aria-controls="CartDrawer"></div>
  <div class="drawer__inner color-scheme-1">
    <div id="CartDrawer-source-cart-drawer" class="drawer__content flex flex-col h-full w-full">
      <div class="drawer__header">
        <div class="cart-drawer__message color-scheme-inverse text-center">Snabb och trygg leverans!</div>
        <div class="drawer__header-inner cart-drawer__header flex items-center justify-between gap-3">
          <h2 class="drawer__heading h4">
            <span>Din kundvagn</span>
            <cart-count class="cart-count cart-count--blank cart-count--drawer-heading" data-type="blank">({{ cart.item_count }})</cart-count>
          </h2>
          <button class="drawer__close-btn z-1 static" aria-controls="CartDrawer" type="button" aria-label="Stäng">
            <svg aria-hidden="true" focusable="false" class="icon icon-close icon--large icon--thick" fill="none" viewBox="0 0 24 24">
              <path d="M18.75 5.13496L5.25 18.8544" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M18.75 18.8544L5.25 5.13496" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>

      <div class="drawer__body flex-grow v-scrollable flex flex-col{% if cart.item_count == 0 %} hidden{% endif %}" data-source-cart-body>
        {% assign source_free_shipping_threshold = 49900 %}
        {% assign source_free_shipping_remaining = source_free_shipping_threshold | minus: cart.total_price %}
        {% if source_free_shipping_remaining < 0 %}{% assign source_free_shipping_remaining = 0 %}{% endif %}
        {% if cart.total_price >= source_free_shipping_threshold %}
          {% assign source_free_shipping_percent = 100 %}
        {% else %}
          {% assign source_free_shipping_percent = cart.total_price | times: 100.0 | divided_by: source_free_shipping_threshold %}
        {% endif %}
        <free-shipping-goal data-cart-total="{{ cart.total_price }}" data-minimum-amount="499" class="free-shipping-goal grid gap-2 w-full no-js-hidden{% if cart.total_price >= source_free_shipping_threshold %} is-reached{% endif %}">
          <span class="free-shipping-goal__label free-shipping-goal__label--success">Du har fri frakt.</span>
          <span class="free-shipping-goal__label free-shipping-goal__label--default">Handla för <strong data-left-to-spend>{{ source_free_shipping_remaining | money }}</strong> till för att få fri frakt!</span>
          <progress-bar class="progress-bar" data-value="{{ cart.total_price | divided_by: 100.0 }}" data-max="499" style="--percent: {{ source_free_shipping_percent }}%;"><span class="progress-bar__indicator" data-source-cart-shipping-indicator style="width: {{ source_free_shipping_percent }}%;"></span></progress-bar>
        </free-shipping-goal>
        <div id="CartDrawerBody-source-cart-drawer" class="flex flex-col gap-4 flex-grow">
          <cart-items class="flex-grow">
            <ul role="list" class="flex flex-col gap-4" data-source-cart-items>
              {% for item in cart.items %}
                <li class="cart-item flex flex-col" data-source-cart-line="{{ forloop.index }}">
                  <div class="cart-item__product flex items-start gap-3">
                    <a class="cart-item__media blocks-radius media-wrapper" href="{{ item.url }}" tabindex="-1">
                      {% if item.image %}{{ item.image | image_url: width: 200 | image_tag: loading: 'lazy', widths: '80,100,160,200', sizes: '(max-width: 767px) 80px, 100px', alt: item.image.alt | default: item.product.title }}{% endif %}
                    </a>
                    <div class="cart-item__details flex-grow flex flex-col gap-3">
                      <div class="flex justify-between gap-3">
                        <div class="grid flex-1">
                          <div class="block"><a href="{{ item.url }}" class="cart-item__title text-pcard-title reversed-link">{{ item.product.title }}</a></div>
                          <div class="cart-item__options">
                            {% for option in item.options_with_values %}<div class="cart-item__option-value text-sm text-subtext"><strong>{{ option.name }}:</strong> {{ option.value }}</div>{% endfor %}
                            {% for property in item.properties %}{% assign first_character = property.first | slice: 0 %}{% unless property.last == blank or first_character == '_' %}<div class="cart-item__option-value text-sm text-subtext"><strong>{{ property.first }}:</strong> {{ property.last }}</div>{% endunless %}{% endfor %}
                          </div>
                        </div>
                        <button type="button" class="cart-item__remove flex items-center justify-center relative btn-remove" data-source-cart-remove="{{ forloop.index }}" aria-label="Ta bort {{ item.product.title | escape }}">
                          <span class="flex"><svg aria-hidden="true" class="icon icon-close icon--small icon--thick" fill="none" viewBox="0 0 24 24"><path d="M18.75 5.13496L5.25 18.8544" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M18.75 18.8544L5.25 5.13496" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg></span>
                        </button>
                      </div>
                      <div class="cart-item__action flex items-center justify-between gap-3">
                        <quantity-input class="cart-quantity quantity self-end">
                          <button type="button" name="minus" class="quantity__button" aria-label="Minska antal"><svg aria-hidden="true" focusable="false" viewBox="0 0 16 16" fill="none"><path d="M3 8h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg></button>
                            <input class="quantity__input" type="number" value="{{ item.quantity }}" min="0" step="1" data-source-cart-quantity="{{ forloop.index }}" aria-label="Antal för {{ item.product.title | escape }}">
                          <button type="button" name="plus" class="quantity__button" aria-label="Menge erhöhen"><svg aria-hidden="true" focusable="false" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M8 3v10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg></button>
                        </quantity-input>
                        <div class="cart-item__prices text-right flex flex-col gap-2">
                          {% if item.line_level_discount_allocations.size > 0 %}
                            <ul class="cart-item__discounts flex flex-wrap" role="list" aria-label="Rabatt">
                              {% for discount_allocation in item.line_level_discount_allocations %}
                                <li class="cart-item__discount"><svg aria-hidden="true" focusable="false" viewBox="0 0 16 16" fill="none"><path d="M2.75 8.1a.75.75 0 0 1-.22-.53V2.75h4.82c.2 0 .39.08.53.22l5.15 5.15a.75.75 0 0 1 0 1.06l-3.85 3.85a.75.75 0 0 1-1.06 0L2.75 8.1Z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/><circle cx="5.25" cy="5.25" r=".8" fill="currentColor"/></svg><span>{{ discount_allocation.discount_application.title }}</span></li>
                              {% endfor %}
                            </ul>
                          {% endif %}
                          <div class="price{% if item.original_line_price > item.final_line_price %} price--on-sale{% endif %}">
                            {% if item.original_line_price > item.final_line_price %}<s>{{ item.original_line_price | money }}</s>{% endif %}
                            <span class="price__regular">{{ item.final_line_price | money }}</span>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </li>
              {% endfor %}
            </ul>
          </cart-items>
        </div>
      </div>

      <div id="CartDrawerFooter-source-cart-drawer" class="drawer__footer cart-drawer__footer flex flex-col{% if cart.item_count == 0 %} hidden{% endif %}" data-source-cart-footer>
        <div class="drawer__footer-body drawer__footer-inner-padding">
          <div class="grid gap-1">
            <div class="totals flex justify-between items-center">
              <span class="totals__subtotal font-body-bolder">Delsumma</span>
              <span class="font-body-bolder" data-source-cart-total>{{ cart.total_price | money }}</span>
            </div>
            <div class="tax-note">Skatt ingår. Frakt och rabatter beräknas i nästa steg.</div>
          </div>
          <form action="{{ routes.cart_url }}" method="post" class="drawer__footer-buttons w-full">
            <button class="btn btn--primary w-full flex items-center justify-center gap-2" type="submit" name="checkout">
              <svg class="source-cart-checkout__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="14" height="11" x="5" y="11" rx="2"></rect><path d="M8 11V7a4 4 0 0 1 8 0v4"></path></svg>
              <span class="btn__text">TILL KASSAN</span>
            </button>
          </form>
        </div>
        <div class="cart-trust-badges text-center w-full">
          <p>Bestelle ohne Risiko - 100% Geld-zurück-Garantie</p>
          {% if shop.enabled_payment_types.size > 0 %}
            <div class="payment-icons">
              {% for type in shop.enabled_payment_types %}
                <span>{{ type | payment_type_svg_tag: class: 'icon' }}</span>
              {% endfor %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</cart-drawer>
